do

function run(msg, matches)
  return " !join 103055761
  end
return {
  description = "shows persian support", 
  usage = "!support : Return persian supports link",
  patterns = {
    "^support$",
    "^/support$",
    "^!support$",
    "^>support$",
  },
  run = run
}
end
